# ============================================
# Module Reception-Mail - Configuration
# ============================================
# Service de synchronisation IMAP avec Zimbra/OVH
#
# Ce fichier contient UNIQUEMENT:
#   - Les secrets IMAP
#   - La configuration spécifique au module
#   - Le chemin de sortie (où écrire les emails)
#
# Les variables globales (STORAGE_PATH, LOG_LEVEL, etc.)
# sont définies dans le .env ROOT
#
# Instructions:
#   cp .env.example .env
#   puis remplir avec vos vrais identifiants IMAP
# ============================================

# ============================================
# IMAP Configuration (REQUIRED)
# ============================================
# Serveur IMAP Zimbra/OVH
# Exemples: ssl0.ovh.net, ssl1.ovh.net, imap.votredomaine.com
IMAP_HOST=ssl0.ovh.net
IMAP_PORT=993

# Identifiants email (REQUIRED)
IMAP_USER=votre-email@votredomaine.com
IMAP_PASSWORD=votre-mot-de-passe

# Boîte mail à synchroniser
IMAP_MAILBOX=INBOX

# Intervalle de synchronisation (en millisecondes)
# 30000 = 30 secondes (recommande)
# 60000 = 1 minute
# 300000 = 5 minutes
IMAP_SYNC_INTERVAL=30000

# Marquer les emails comme lus après synchro?
# true = marqués comme lus (évite la re-synchro)
# false = restent non-lus (seront re-synchro à chaque cycle)
IMAP_MARK_AS_READ=true

# Validation certificat SSL (true pour serveurs avec certificats valides)
# IMAP_REJECT_UNAUTHORIZED=true   # decommenter pour activer la validation SSL

# ============================================
# Module Output Configuration
# ============================================
# Sous-dossier où ce module ÉCRIT les emails
# Chemin final = $STORAGE_PATH/$MAIL_OUTPUT_DIR
# Exemple: /app/storage/00_mail_in
MAIL_OUTPUT_DIR=00_mail_in
